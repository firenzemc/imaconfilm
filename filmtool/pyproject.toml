[project]
name = "filmtool"
version = "0.1.0"
description = "Imacon FFF film-negative developer (auto crop + orange-mask removal)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "numpy>=1.26",
    "pillow>=10.0",
    "tifffile>=2024.1.30",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
